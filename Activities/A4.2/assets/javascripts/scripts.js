//ADDS FOCUS CLASS TO AN ELEMENT
function addFocus(element) {
	element.parentElement.classList.add("focus");
}

//REMOVES FOCUS CLASS TO AN ELEMENT
function removeFocus(element) {
	element.parentElement.classList.remove("focus");
}